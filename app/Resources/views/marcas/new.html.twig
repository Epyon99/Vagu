{% extends 'base.html.twig' %}

{% block body %}
    <h1>Marca creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input class="btn" type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('marcas_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
